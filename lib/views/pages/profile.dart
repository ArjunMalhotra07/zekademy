import 'package:flutter/cupertino.dart';

class ProfileBarPage extends StatelessWidget {
  const ProfileBarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
